# Tmux Snapshot Resource Counts

## Summary

The tmux golden snapshots now match the current bundled resource inventory.

## Contract

Home, help, and post-send tmux snapshots must reflect the resource counts loaded
from the current bundled resources. The visible model status line reports
`tools 84/92`, and wide help reports `prompts 14`, `tools 92`, and `skills 16`.

## Compatibility

This is a test fixture update only. It does not change TUI rendering logic or
resource loading behavior.
