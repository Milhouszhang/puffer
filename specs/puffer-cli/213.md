# Contact Context Scope Guard

## Summary

`contacts_context` now rejects requests whose `contact_ids` normalize to an
empty list. Empty or malformed contact scopes no longer fall through to the
internal all-candidate context path.

## Compatibility

Valid mixed requests still normalize and use the routable contact ids. Invalid
or empty requests now return a daemon error instead of broad contact context.
