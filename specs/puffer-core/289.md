# puffer-core 289: RunAutomation workflow helper labels

## Summary

Workflow command helpers now recognize `ActionSpec::RunAutomation` when
rendering binding action metadata.

## Behavior

`run_automation` is reported as its own action type and displays
`automation=<id>` as the target. It remains distinct from `run_workflow`, which
continues to display AgentEnv runtime workflow ids.
