# Automation Home CTA Copy

## Summary

The Automation home now uses shorter CTA labels for the library actions and a
Chinese empty-state sentence requested for the first automation moment.

## Behavior

- The toolbar action label is `new`.
- The empty-state sentence is `创建你的第一个automation，处理重复的工作流`.
- The empty-state CTA label is `create automation`.
- The automation creation behavior is unchanged.

## Testing

`tests/automation-ui.spec.ts` verifies the updated toolbar action, empty-state
sentence, and empty-state CTA label.
