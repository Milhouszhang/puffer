# Workflow Popup Contact Defaults

- Workflow popup tests that construct `WorkflowBindingSpec` directly initialize
  `contact_ids` to an empty list.
- Empty contact ids mean all contacts, preserving the existing delete/autocomplete
  popup behavior for workflow bindings that do not expose contact scoping in the TUI.
