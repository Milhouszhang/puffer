# Workflow filter labels

Workflow action popups now render compound `any` filters by joining their child
filter labels with `||`. This keeps the TUI compatible with monitor include
rules that are stored as multiple keyword filters while preserving the existing
labels for regex, jq, JSON, and empty filters.
