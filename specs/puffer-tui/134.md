# puffer-tui 134: RunAutomation action labels

## Summary

The workflow action popup now recognizes `ActionSpec::RunAutomation` as a
separate action type.

## Behavior

`run_automation` rows display `automation=<id>` as the target. The existing
`run_workflow` display remains reserved for AgentEnv runtime workflow ids.
